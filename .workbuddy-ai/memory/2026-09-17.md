# 2026-09-17

## ETF T+0 策略「已启用但不实盘交易」排查结论

诊断对象：`config/strategies/etf_t0.yaml`（enabled=true，标的 513100.SH / 159941.SZ）
代码：`qmt_trade/strategies/etf_t0.py::ETFT0LiveRunner`，调度任务 `etf_t0_intraday`（每 30s，09:30~15:00）

### 五层卡点（按致命程度排序）
1. **后端进程没运行**：`logs/backend.log` 最后写入 2026-09-16 16:39，9/17 全天 0 条；
   端口 7099 无监听；`job_runs` 最后 trade_date=2026-09-16。
2. **scheduler.mode=paper**（`config/settings.yaml`）：paper 下 `ctx.gateway=SimGateway`，
   结构上不下真单。LIVE 账本 `trading_live_e7e407d435357cb5` 里 ETF 订单 0 笔、持仓 0 只；
   1261 笔 ETF_T0_* 全落在 PAPER 账本 `trading_paper_382635c9325bf327`。
   `config/.env` 已设 `QMT_ALLOW_LIVE=1`，解锁条件已满足，只差改 mode=live。
3. **底仓建仓长期被 Gate-1 拒**：ETF_T0_BASE 累计 1259 笔 REJECTED。
   拒因分布 —— 605 笔「KillSwitch=REDUCE_ONLY」（9/15 23:58 人工恢复 NORMAL）、
   640 笔「总仓位已达 Regime 上限 20%」（regime=TREND_DOWN, max_position=0.2）、
   少量「sizer 取整后不足 100 股」。9/16 10:34 才首次 FILLED 2 笔。
4. **T+1 可卖数量锁死**：建仓当日 `positions.available=0`，先卖后买无法开腿；
   同期 REGIME_CUT 卖这两只也被拒 339×2 笔「无可卖数量（T+1）」。
   `mark_t1` 只在主策略 `intraday` 任务里调用（jobs.py:897），ETF T+0 自己不调。
5. **开腿信号未触发**：9/15、9/16 两天 `legs_opened` 全为 0（943 条 [OK] 记录）。
   门槛：dv≥0.008（现价高于全日 VWAP）+ momentum_mode=filter（15 分钟涨幅 <0.004）+ 09:35~14:30 窗口。

### 已确认的代码/配置缺陷
- **`base_fraction_override` 是死配置**：`etf_t0.py:159` 声明后全仓无任何其他引用。
  yaml 里 `513100.SH: 0.2 / 159941.SZ: 0.8` 完全无效；实盘 `_ensure_base` 实际用
  `max(0.02, min(0.15, cfg.base_fraction))`=0.12，两只都建了约 12 万元（各 12%）。
- **与 Regime 上限设计冲突**：159941 想配 80%，但 TREND_DOWN 下 max_position=0.2，永远被拒。
- **可观测性缺陷**：`_open_leg` 先判 dv 再判 `can_use`，dv 不满足直接 return "skipped" 且不计数，
  于是被 T+1 卡死时日志 `legs_opened=0 且 rejected=0`，看不出真实原因。

### 待办 / 风险
- 当前 paper 组合：cash 759,729 / market_value 241,816 / total 1,001,546 → 仓位 24.1% 已超
  TREND_DOWN 的 20% 上限。**若今天启动后端，主策略 REGIME_CUT 大概率会立刻卖掉这两只 ETF 底仓**，
  与 ETF T+0 的底仓意图直接冲突，需先决策。

### 排查手法（可复用）
- 日志聚合：`logs/backend.log` 里 `[OK  ] etf_t0_intraday ... legs_opened=N legs_closed=N fills=N rejected=N base=N`
  可直接按字段统计，一眼看出策略是否真的在动。
- 账本查询：duckdb `data/db/qmt.duckdb`，schema 按 `trading_{mode}_{sha256(account)[:16]}`
  隔离（见 `qmt_trade/storage/runtime.py`），paper/live 各一套，别查错账本。
- 拒因定位：`orders.reject_reason` 按 signal+status 分组统计，能直接定位卡在哪道风控。

## 当日修复（etf_t0.py，134 增 40 删）

1. **`base_fraction_override` 死配置修复**：新增 `ETFT0Config.base_fraction_for(sym)`
   （override 优先，未配置回落 base_fraction）；回测 `_enter_base` 与实盘 `_ensure_base`
   都改为调用它。实盘 clamp 从 `max(0.02,min(0.15,...))` 放宽为 `max(0.01,min(1.0,...))`，
   超出部分交给 Gate-1/Regime 明确拒单并留可读拒因，不再被悄悄截断。
2. **开/平腿原因可观测**：`_open_leg` → `tuple[str,str]`、`_close_leg` → `tuple[bool,str]`；
   新增 `_res_reason`（三态归一）与 `_bump`（数字折 # 聚合）；summary 新增
   `open_reject_reasons / open_skip_reasons / close_fail_reasons`；平腿"未到条件"是常态不记，
   只记真被拒。tick 开头补 `portfolio.mark_t1(today)`，ETF T+0 不再依赖主策略 intraday 解冻 T+1。

测试：`test_scheduler_noise_fix` + `test_durable_backtests` 等 15 项通过；
`test_strategy_instances_api::test_save_draft_then_publish_and_rollback` **是预存在失败**
（git checkout 还原到 HEAD 后同样失败），与本次改动无关。

## 后续待决策（未实施）

- **scheduler.mode 改不改 live**：`config/settings.yaml::scheduler.mode`，也可经
  系统设置→参数配置（`server/routers/config.py::/config/patch`，前端 `ConfigView.vue`
  是通用编辑器）改。**改完必须重启后端**——`_resolve_scheduler_mode()` 只在 startup 求值一次。
- **live 账户真值（9/15 快照）**：total 182,732.39 / cash 53,608.39 / mv 129,124 → **仓位 70.7%**，
  持仓 600216.SH 1000 股、300570.SZ 600 股，**没有 513100/159941 底仓**。
  切 live 当天 REGIME_CUT 会按 shrink≈0.717 卖掉 700 股 600216 + 400 股 300570（真金白银）。
- **Regime 减仓规则出处**：`qmt_trade/risk/engine.py::_regime_deleverage`，
  cap=regime.max_position（TREND_DOWN 0.2），`mv > cap*total*1.02` 触发，
  `shrink = 1 - cap*total/mv`，每只等比例减、剩余<200 股则清。不区分标的来源。
- **建议**：不给 ETF 底仓开 Regime 豁免（那是核心风控），应把 ETF 底仓纳入统一仓位预算
  （TREND_DOWN 下两只合计 ≤20%，别配 12%+12% 或 20%+80%），或只在 RANGE/TREND_UP 启用。

## 「规则统一」改造（用户拍板：不豁免，但要把口径统一、消除规则打架）

改了 4 个文件 +237/-50。**核心是消灭三处各算各的市值口径**。

1. **`PortfolioState.market_value(last_prices=None)`**（portfolio/state.py）
   新增，作为**风控唯一的市值真相源**。价格优先级：外部 last_prices > 持仓 last_price > avg_cost。
   `position_value` 改为委托给它。
2. **`risk/engine.py`**
   - 新增模块常量 `REGIME_CAP_BUFFER = 1.02`，Gate-1/Gate-2 共用（此前 Gate-2 硬编码 1.02，Gate-1 没有）。
   - **Gate-1 从 `sum(p.shares*p.avg_cost)` 改为 `portfolio.market_value()`** —— 这是最关键的修复：
     此前 Gate-1 用成本价、Gate-2 用现价，浮盈时 Gate-1 认为没超限而放行买入、Gate-2 转头
     判定超限立刻减仓，造成「刚建仓就被 REGIME_CUT 砍」的自相矛盾。
   - `check_pre_trade` 新增 `net_new_exposure: bool = True`；False 时跳过总仓位校验。
   - `_regime_deleverage` 改用 `portfolio.market_value(last_prices)` + 同一缓冲常量。
3. **`execution/service.py`**
   新增 `BUYBACK_SIGNALS`（ETF_T0_BUYBACK / STOP_BUYBACK / FORCE_FLAT、STOCK_T0_* 、STOP_LOSS），
   BUY 且 signal 在其中时传 `net_new_exposure=False`。
   语义：**回补当日已卖出的份额不算新增敞口** —— 否则「允许先卖后买，却不允许买回来」，
   T 仓平不掉、尾盘强平补不回，底仓被风控一点点吃掉。ETF_T0_BASE 不在集合内（建底仓是新增敞口）。
4. **`strategies/etf_t0.py::_ensure_base`** 建仓前按同一口径自检：
   `headroom = cap - market_value/total`；意图 > headroom 就收敛到 headroom（写 WARNING + 计入
   summary.base_reject_reasons），headroom<=0 直接不建。收敛到 cap（不含 1.02 缓冲），
   保证建完不会触发 Gate-2 减仓。
5. **`config/strategies/etf_t0.yaml`** 加注释说明 0.8 只是意图上限、会被 Regime 额度收敛
   （注意 `config/` 整目录在 .gitignore，改动不入 git）。

验证脚本结论：cap=20% 时 —— 当前仓位 10% → 收敛到 10%；19% → 收敛到 1%；25% → 额度 0 不建；
cap=50%、当前 10% → 收敛到 40%。浮盈场景 Gate-1/Gate-2 结论已一致。

全量测试：105 passed，1 failed（`test_strategy_instances_api::...rollback`，**预存在失败**）。
`tests/test_datahub_quality.py` 是脚本式文件（import 即 sys.exit），会让 pytest INTERNALERROR，
跑全量时要 `--ignore` 掉。

**仍未做**：scheduler.mode 要不要改 live（会触发 live 账户 70.7% 仓位的 REGIME_CUT）。

## 仓位参数自洽性审计（用户：这些指标不要互相矛盾，要统一一致）

新增 `qmt_trade/core/budget_audit.py`，挂在 `get_settings()` 上（`audit_once`，进程内只跑一次）。
**只读告警，绝不改值、绝不改交易行为** —— 让"各自合法、组合起来打架"在启动时就暴露。

当前审计出 **6 处冲突**（都是真矛盾，不是误报）：
1. `max_positions(8) × max_single_weight(12%) = 96%` > Regime 天花板 80% → 永远建不满 8 只
2. `equal_weight=true` + `target_positions=8` → 理想 12.5% 被 `max_weight_pct=12%` 压到 12%，**从未等权过**
3. `max_order_value_ratio(10%)` < `max_single_weight(12%)` → 单票建满要 2 次委托
   （且该参数**只在 exact_buy 精确回补路径生效**，常规路径走 sizer，两条路上限不同）
4. `cash_buffer` 同名两个值：sizer 用 `1-cash_usage_ratio`=5%，risk 用 `gate1.cash_buffer`=0.5%
5. ETF T+0 `base_fraction_override` 20%/80% 超 `max_single_weight` 12%，实际只建 12%
6. ETF T+0 底仓意图合计 100% > Regime 天花板 80%

## 方案 A 已落地（用户确认：单票 ≤12%，只买 3~4 只）

`config/settings.yaml` 实际改动：
| 参数 | 旧 → 新 | 理由 |
|---|---|---|
| `risk.gate1.max_positions` | 8 → **4** | 8×12%=96% 永远达不到；用户实际只买 3~4 只 |
| `risk.gate1.max_order_value_ratio` | 0.10 → **0.12** | 与 max_single_weight 取齐，单票一笔建满不再分批 |
| `risk.gate1.cash_buffer` | 0.005 → **0.05** | 与 sizer 的 `1-cash_usage_ratio` 统一 |
| `portfolio.equal_weight` | true → **false** | 见下 |
| `portfolio.target_positions` | 8 → **4** | 与 max_positions 一致 |
| `strategies.etf_t0.base_fraction_override` | 0.2/0.8 → **0.12/0.12** | 单票 12% 封顶 |

**为什么关 equal_weight**（关键判断）：只买 3~4 只时"等权"意味着每只 1/4=25%，
与单票上限 12% 直接冲突，实际每只都被压到 12%——配置说等权、行为从不等权。
单票 12% + max_positions 4 已完整表达集中度控制，不需要再叠一层失效的等权。

**实测影响**（live 账户 182,732，用 ETF T+0 的 risk_budget_hint=0.3 探测）：
- 旧（equal_weight=true）：每只约 **200 股 / 4,000 元 / 2.2%**，bounded_by=equal_weight
  （被 `risk_guard_notional = total × total_risk_budget(0.03) / stop_dist` 卡住）
- 新（equal_weight=false）：每只约 **500 股 / 10,000 元 / 5.5%**，bounded_by=risk_budget
- 结论：**12% 是硬天花板，不是目标规模**；实际下单量由 `base_risk_pct`(0.6%)
  × conviction × regime_factor 决定，通常落在 2%~6%。想买更满要调 base_risk_pct
  或 intent 的 risk_budget_hint，不是调 max_single_weight。

改完重跑审计：**未发现冲突**（6 → 0）。测试 105 passed / 1 failed（预存在）。

**遗留**：`brain.final_picks_max=8` 仍大于 max_positions=4（选股候选池 vs 持仓上限），
以及 `selection.ranker.top_n=100` —— 属选股层，不冲突（反正每天最多新开 3 只），未改。

用户明确：**scheduler.mode 他自己手动切；仓位他自己过两天人工降**。不要再替他改这两项。

## 单票上限 12% → 20%（用户追加要求，已落地）

`config/settings.yaml`：
| 参数 | 12% 方案 → 20% 方案 |
|---|---|
| `risk.gate1.max_single_weight` | 0.12 → **0.20** |
| `portfolio.max_weight_pct` | 0.12 → **0.20**（必须与上者同值） |
| `risk.gate1.max_order_value_ratio` | 0.12 → **0.20**（取齐，一笔建满） |
| `risk.gate1.max_industry_weight` | 0.30 → **0.40**（用户选：同行业允许 2 只） |
| `strategies.etf_t0.base_fraction_override` | 0.12/0.12 → **0.20/0.20**（用户选：两只顶格） |

`max_positions` 保持 4 → 4×20% = 80%，正好等于 TREND_UP 的 Regime 天花板（不越界）。

**顺带给 budget_audit 补了一条检查**：`max_industry_weight / max_single_weight < 2` 时告警
「行业容量不足」——20% 单票 + 30% 行业只能放 1 只，第 2 只同行业票会被 Gate-1 拒，
这个隐性约束原先审计抓不到。

**关键认知（写给用户也写给未来的自己）**：单票上限是**天花板不是目标规模**。
实测 live 账户下实际每笔约 5.5%（bounded_by=risk_budget），上限根本没触及。
提高上限的真正意义是：**止损距离小的票能买更多**（raw_shares = risk / stop_dist，
止损越近股数越多，原来 12% 会把它们卡住）。要整体买更满应调
`portfolio.base_risk_pct`（0.006）或 intent 的 `risk_budget_hint`，而不是调上限。

最终审计：**未发现冲突**。测试 105 passed / 1 failed（预存在）。

**风险提示（已告知用户）**：4×20%=80% 意味着 TREND_UP 下可满仓，单票集中度比 12% 时高 67%。

## ETF T+0 底仓 12% vs 20% 回测对比（用户要求，已完成）

新增脚本 `tests/bt_etf_t0_basefrac.py`（支持 --fracs / --grid-steps / --start / --end / --cash），
结果追加到 `logs/etf_t0_basefrac.txt`，报告 `reports/backtest_etf_t0_basefrac_20260917.md`。

**1年区间（2025-08-18~2026-08-18，初始100万，513100.SH+159941.SZ）**
| 底仓 | 总收益 | 最大回撤 | 夏普 | T0净盈亏 | 腿数 | 胜/负 |
|---|---|---|---|---|---|---|
| 12% | 6.62% | -4.51% | 1.09 | -345 | 30 | 14/16 |
| 20% | 11.06% | -7.15% | 1.12 | -575 | 30 | 14/16 |

**关键认知1：收益 = 底仓 beta 的线性缩放，不是 alpha。**
反算底仓自身收益率：240k→66.2k (27.6%)、400k→110.6k (27.65%)，几乎完全相等。
腿数完全相同（30腿）→ 底仓大小不改变 T0 触发频率，只改变每腿名义。
20% 赢只是因为样本期两只 ETF 单边上涨放大了 beta；下跌段亏损同样按 1.67× 放大。

**关键认知2（更重要）：T0 腿在默认参数下结构性亏损，与底仓档位无关。**
往返成本 = base_slippage 0.002×2 + commission 0.0001×2 + transfer 1e-5×2 ≈ **0.42%**。
grid_step 0.005、stop_pct 0.005 看似对称，但成本把赢腿吃光：
赢腿 ≈ 0.5%−0.42% = +0.08%，输腿 ≈ −(0.5%+0.42%) = −0.92%，**盈亏比 1:11，需 >85% 胜率才打平**，
实测胜率仅 47%。

**grid_step 敏感性（3个月 / 1年双重验证，方向一致）**
| grid_step | T0净盈亏(3M,12%) | 腿数 | 胜/负 |  | T0净盈亏(1Y) 12%/20% |
|---|---|---|---|---|---|
| 0.003 | **+690** | 20 | 12/8 |  | **+1093 / +1825** (32腿 19/13) |
| 0.005 | -438 | 18 | 7/11 |  | -345 / -575 |
| 0.008 | -590 | 18 | 7/11 |  | — |

**网格越窄越赚，且是方向性翻转不是渐变**（胜率 47% → 60%）。
原因：腿利润来自回归 VWAP（close_leg_dev），网格放宽只是让腿持仓更久、暴露在趋势延续里。

**待用户决定的建议（未擅自改 config）**：
1. `grid_step: 0.005 → 0.003`：零成本，1年 T0 由负转正，额外 +0.10~0.16pp。
2. `base_slippage: 0.002 → 0.0005`：宽基 ETF 实际滑点 0.01%~0.05%，往返成本 0.42%→0.12%，
   盈亏比根本性改善；但属保守假设，调低会虚高回测，改前需先用实盘成交记录校准。

未做的事（用户明确要求）：`scheduler.mode` 未改（他自己切）；仓位未动（他自己降）。

### 后续：grid_step 0.005 → 0.003 已落地（用户"继续"确认）

`config/strategies/etf_t0.yaml` 已改，并写清机理注释。回归测试 105 passed / 1 failed
（`test_save_draft_then_publish_and_rollback`，预存在，已用 git checkout 验证过）。

**修正一条我自己先前的错误推导（重要，别再犯）：**
我一开始用「往返成本 0.42%（滑点+佣金+印花税）> 网格 0.5% 的毛收益 → 盈亏比 1:11」来解释 T0 亏损。
**这是错的**，两个原因：
1. `t0_pnl` 是**价差口径**：含 SimGateway 滑点（±0.2%，往返 0.4%），但**不含**佣金/印花税/过户费
   —— 那三项走 `portfolio.apply_fill`，计入总权益而非腿级盈亏。代码见
   `etf_t0.py` 的 `st["day_pnl"] += (leg_px - fill_price) * qty`。
2. 实测网格止盈腿单腿均值就是**正的**（+150~251），根本不是被成本吃掉。

**真实机理（加了按平腿原因的分项统计才看清）**：
| | 止盈腿 | 止损腿 | 强平腿 | 胜率 |
|---|---|---|---|---|
| g=0.003 | 12 | 4 | 4 | 60% |
| g=0.005 | 5 | 7 | 6 | 39% |

网格越宽 → 腿持仓越久 → 原本能落袋的腿漂成反向止损（-190~-322/腿）。
另外实测捕获价差约 0.8%（≈ sell_dev_threshold），远大于 grid_step 本身 —— 分钟线离散，
触发那根 bar 的价格往往已越过网格位不少，所以**窄网格也能赚**。

**新增可观测性（只加日志，不动交易逻辑）**：
`ETFT0Backtester._leg_log` + `_log_leg(tag, pnl, qty)`，回测 `run()` 聚合出
`metrics["t0_by_tag"]`（按腿数排序），bt 脚本会打印分项表。
SELL 腿把原 `px <= leg_px*(1-g) or dv <= close_leg_dev` 拆成两个分支分别
打「回归VWAP买回 / 网格止盈买回」标签 —— **行为完全等价**（都在 px 成交），只是标签不同。

**踩坑**：`_close_leg` / `_res_reason` / `_bump` 属于 `ETFT0LiveRunner`（line~613+），
**不**属于 `ETFT0Backtester`（line 205-612，有自己的内联 `_intraday_t0` 平腿逻辑）。
我第一次把 `_log_leg` 加到 `_res_reason` 前面 → AttributeError。
改回测必须改 `ETFT0Backtester._intraday_t0` 里内联的那段。

**诚实的量级结论**：grid_step 修正只值 +0.10~0.16pp/年。T0 对总业绩贡献就是 0.1% 级，
调参改变不了「这策略本质是持有 ETF，做T只是噪声」。要提升得改信号本身。

**未采纳**：`base_slippage` 0.002→0.0005（会让回测虚高，须先用实盘成交校准）；
`stop_pct` 收紧（第二大漏点，需单独做敏感性测试）。
